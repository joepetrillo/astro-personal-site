---
type Props = {
  italic?: boolean;
};

const { italic } = Astro.props;
---

<div
  class:list={[
    "inline-block rounded-sm bg-zinc-100 px-1.5 py-1.5 font-mono text-sm text-zinc-600",
    italic && "italic",
  ]}
>
  <slot />
</div>
